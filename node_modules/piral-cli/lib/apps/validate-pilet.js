"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
const common_1 = require("../common");
const rules_1 = require("../rules");
exports.validatePiletDefaults = {
    entry: './src/index',
    logLevel: 3,
};
function validatePilet(baseDir = process.cwd(), options = {}) {
    return __awaiter(this, void 0, void 0, function* () {
        const { entry = exports.validatePiletDefaults.entry, logLevel = exports.validatePiletDefaults.logLevel, app } = options;
        const rules = yield rules_1.getPiletRules();
        const entryFile = path_1.join(baseDir, entry);
        const target = path_1.dirname(entryFile);
        const _a = yield common_1.retrievePiletData(target, app), { dependencies, peerDependencies, devDependencies, root } = _a, data = __rest(_a, ["dependencies", "peerDependencies", "devDependencies", "root"]);
        const { validators } = common_1.getPiletsInfo(data.appPackage);
        const errors = [];
        const warnings = [];
        const context = {
            error(message) {
                errors.push(message);
            },
            warning(message) {
                warnings.push(message);
            },
            logLevel,
            entry: entryFile,
            dependencies,
            devDependencies,
            peerDependencies,
            root,
            data,
        };
        yield common_1.runRules(rules, context, validators);
        common_1.ruleSummary(errors, warnings);
    });
}
exports.validatePilet = validatePilet;
//# sourceMappingURL=validate-pilet.js.map