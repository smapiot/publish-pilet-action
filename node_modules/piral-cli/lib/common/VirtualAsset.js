"use strict";
const Bundler = require("parcel-bundler");
const hash_1 = require("./hash");
class VirtualAsset extends Bundler.Asset {
    constructor(name, options) {
        super(name, options);
        const refName = name.substr(1).replace(/\.vm$/, '');
        this.content = `module.exports=require('${refName}');`;
    }
    load() { }
    generate() {
        return {
            js: this.content,
        };
    }
    generateHash() {
        return Promise.resolve(hash_1.computeMd5(this.content));
    }
}
module.exports = VirtualAsset;
//# sourceMappingURL=VirtualAsset.js.map