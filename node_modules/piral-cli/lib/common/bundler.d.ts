import { ParcelBundle } from 'parcel-bundler';
export declare function openBrowser(shouldOpen: boolean, port: number): Promise<void>;
export declare function extendBundlerWithPlugins(bundler: any): void;
export declare function clearCache(root: string, dir?: string): Promise<void>;
/**
 * Changes the files from the generated bundle to *really*
 * follow the desired preset environment - which includes also
 * packages coming from node_modules.
 * @param mainBundle The main bundle coming from Parcel.
 * @param rootDir The project's root dir.
 */
export declare function postTransform(mainBundle: ParcelBundle, rootDir: string): Promise<void[]>;
