"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
const io_1 = require("./io");
const npm_1 = require("./npm");
function createPiletPackage(baseDir, source, target) {
    return __awaiter(this, void 0, void 0, function* () {
        const root = path_1.resolve(baseDir, source);
        const dest = path_1.resolve(baseDir, target);
        const pckg = yield io_1.readJson(root, 'package.json');
        if (!pckg) {
            console.error('No valid package.json found.');
            throw new Error('Invalid pilet.');
        }
        if (!pckg.name) {
            console.error('Cannot pack the pilet - missing name.');
            throw new Error('Invalid pilet.');
        }
        if (!pckg.version) {
            console.error('Cannot pack the pilet - missing version.');
            throw new Error('Invalid pilet.');
        }
        console.log(`Packing pilet in ${path_1.resolve(baseDir, target)} ...`);
        yield npm_1.createPackage(root);
        const name = `${pckg.name}-${pckg.version}.tgz`.replace(/@/g, '').replace(/\//g, '-');
        const file = path_1.join(root, name);
        if (dest !== root) {
            return yield io_1.move(file, dest, io_1.ForceOverwrite.yes);
        }
        return file;
    });
}
exports.createPiletPackage = createPiletPackage;
//# sourceMappingURL=pack.js.map