"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const ejs_1 = require("ejs");
const path_1 = require("path");
const io_1 = require("./io");
function fillTemplate(type, name, data = {}) {
    const path = path_1.resolve(__dirname, '..', '..', 'templates', type, `${name}.ejs`);
    return new Promise((resolve, reject) => {
        ejs_1.renderFile(path, data, (err, str) => {
            if (err) {
                reject(err);
            }
            else {
                resolve(str);
            }
        });
    });
}
exports.fillTemplate = fillTemplate;
function createFileFromTemplateIfNotExists(type, prefix, targetDir, fileName, forceOverwrite, data) {
    return __awaiter(this, void 0, void 0, function* () {
        const content = yield fillTemplate(type, `${prefix}-${fileName}`, data);
        yield io_1.createFileIfNotExists(targetDir, fileName, content, forceOverwrite);
    });
}
exports.createFileFromTemplateIfNotExists = createFileFromTemplateIfNotExists;
//# sourceMappingURL=template.js.map