"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const log_1 = require("./log");
function ruleSummary(errors, warnings) {
    log_1.logInfo('');
    log_1.logInfo(`Found ${warnings.length} warning(s) and ${errors.length} error(s).`);
    log_1.logInfo('');
    warnings.forEach(warning => log_1.logWarn(warning));
    errors.forEach(error => log_1.logFail(error));
    if (errors.length > 0) {
        throw new Error(`Please fix the ${errors.length} error(s).`);
    }
}
exports.ruleSummary = ruleSummary;
function runRules(rules, context, configurations = {}) {
    return __awaiter(this, void 0, void 0, function* () {
        for (const rule of rules) {
            const options = configurations[rule.name];
            yield rule.run(context, options);
        }
    });
}
exports.runRules = runRules;
//# sourceMappingURL=rules.js.map