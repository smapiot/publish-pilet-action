"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
function getPath() {
    try {
        return path_1.dirname(require.resolve('piral-cli/lib', {
            paths: [process.cwd()],
        }));
    }
    catch (_a) {
        return path_1.dirname(__filename);
    }
}
function select(from) {
    return __awaiter(this, void 0, void 0, function* () {
        const path = getPath();
        const { loadPlugins } = require(path_1.join(path, 'plugin'));
        const { commands } = require(path_1.join(path, 'commands'));
        const { setupCli } = require(path_1.join(path, 'cli'));
        yield loadPlugins();
        yield setupCli(from(commands));
    });
}
exports.select = select;
//# sourceMappingURL=select.js.map